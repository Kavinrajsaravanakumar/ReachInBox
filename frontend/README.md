# ReachInbox web app (Next.js)

Run from this folder:

```bash
npm install
npm run dev
```

Opens at http://localhost:3001

Point the API `OAUTH_SUCCESS_REDIRECT` at `http://localhost:3001/auth/callback` so Google/Slack can return a token to the UI.
